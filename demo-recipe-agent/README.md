# Recipe Agent (demo, originally generated by Claude)

First, install uv:

```shell
curl -LsSf https://astral.sh/uv/install.sh | sh
```

Then run the app:

```shell
export ANTHROPIC_API_KEY='your-api-key-here'

uv run agent.py
```
